node_modules/
.next/
out/
build/
dist/

.env
.env.local
.env.*.local
# Edge config for the self-hosted stack. The `.env` line above is an exact match
# and does not cover this one, so it needs its own entry.
.env.caddy

# Database dumps written by scripts/backup-db.sh
backups/

*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.DS_Store
Thumbs.db

next-env.d.ts
tsconfig.tsbuildinfo
.vercel
snapshots/

# Phase brief files at the repo root: working notes for one contributor's own
# reference, deliberately never committed (they were briefly re-added by
# 0a0ea12 and untracked again in the audit that reviewed that commit).
# AI_POSTER_HANDOFF.md links to some of these by name; the links stay valid
# locally even though the files themselves are not in git.
/vertical*.md
